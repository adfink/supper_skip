<h1> Create Your New Category </h1>

<%= form_for ([@restaurant, @category]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :description %>
  <%= f.text_field :description %><br>

  <%= f.submit "Create Category" %>
<% end %>
